import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { Toast } from './Toast.tsx'

<Meta title="MDX/Toast" />

# Toast

## Basic Usage

<Canvas>
  <Story name="Basic Usage">
    <Toast visible hideAfterSeconds={90}>
      {({ countdown, state }) => {
        return (
          <p>
            This is a toast: {state}. {countdown}
          </p>
        )
      }}
    </Toast>
  </Story>
</Canvas>
