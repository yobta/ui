import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { bulk } from './bulk.ts'

<Meta title="MDX/Helpers/Bulk" />

# Bulk

<!-- add a plugin description -->

<Canvas>
  <Story name="Bulk Usage">
    {() => {
      let str = ''
      let func = () => (str = str + 'one')
      let func1 = () => (str = str + ' ' + 'two')
      let func2 = () => (str = str + ' ' + 'three')
      bulk(func, func1, func2)
      return (
        <div className="yobta-bg-paper-2 w-max">
          <p>let str = ''</p>
          <p>let func = () => (str = str + 'one')</p>
          <p>let func1 = () => (str = str + ' ' + 'two')</p>
          <p>let func2 = () => (str = str + ' ' + 'three')</p>
          <p>bulk(func, func1, func2)</p>
          <p className="yobta-bg-paper yobta-ink-2">// str = '{str}'</p>
        </div>
      )
    }}
  </Story>
</Canvas>
